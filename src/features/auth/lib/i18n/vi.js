import {
  viMessages as loginMessages,
} from './loginPage';
import {
  viMessages as registerMessages,
} from './registerPage';

const messages = {
  ...loginMessages,
  ...registerMessages,
};

export default messages;
