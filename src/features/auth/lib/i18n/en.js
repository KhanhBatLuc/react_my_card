import { enMessages as loginMessages } from './loginPage';
import { enMessages as registerMessages } from './registerPage';

const messages = {
  ...loginMessages,
  ...registerMessages
};

export default messages;
